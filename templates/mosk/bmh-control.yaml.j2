# MOSK Control Plane BareMetalHostInventory
# Generated by MCC/MOSK Virtual Deployment
# Since MOSK 2.29.0+, use BareMetalHostInventory instead of BareMetalHost
{% for node in mosk_control_nodes %}
---
apiVersion: kaas.mirantis.com/v1alpha1
kind: BareMetalHostCredential
metadata:
  name: bm-mosk-ctl-{{ '%02d' | format(node.index) }}-credentials
  namespace: {{ mosk_namespace }}
  labels:
    kaas.mirantis.com/provider: baremetal
spec:
  username: {{ bmc_username }}
  password:
    value: {{ bmc_password }}
---
apiVersion: kaas.mirantis.com/v1alpha1
kind: BareMetalHostInventory
metadata:
  name: bm-mosk-ctl-{{ '%02d' | format(node.index) }}
  namespace: {{ mosk_namespace }}
  labels:
    kaas.mirantis.com/baremetalhost-id: hw-mosk-ctl-{{ '%02d' | format(node.index) }}
    kaas.mirantis.com/provider: baremetal
  annotations:
    inspect.metal3.io/hardwaredetails-storage-sort-term: hctl ASC, wwn ASC, by_id ASC, name ASC
spec:
  bmc:
    address: {{ kvm_node_ip }}:{{ node.vbmc_port }}
    bmhCredentialsName: bm-mosk-ctl-{{ '%02d' | format(node.index) }}-credentials
  bootMACAddress: {{ node.mac_address }}
  online: true
{% endfor %}
